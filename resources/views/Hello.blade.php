<!doctype html>
<html lang="zh">
<head>
    <meta charset="UTF-8">
    <title>{{$title}}</title>
</head>
<body>
    <h1>{{$title}}</h1>
    @if($title=='hello')
        哈喽
    @else
        欢迎你
    @endif

</body>
</html>
